{
  "name": "officeriders-status-page",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Public status page for OfficeRiders services.",
  "license": "EUPL-1.2",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "lint": "biome check .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "build": "npm run typecheck && npm run test && npm run leakage",
    "leakage": "tsx scripts/leakage-guard.ts"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.3",
    "@types/node": "26.1.1",
    "tsx": "4.23.0",
    "typescript": "7.0.2",
    "vitest": "4.1.10"
  }
}
